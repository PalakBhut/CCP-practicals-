#include<stdio.h>
void main()
{
    int length,breadth,area,perimeter;
    length=50;
    breadth=30;
    area=length*breadth; //calculate the area
    printf("the area of rectangle is %d" ,area);
    perimeter=2*(length+breadth); //calculate the perimeter
    printf("\nthe perimeter of the rectangle is %d" ,perimeter);
    printf("\n24CE016_Palak Bhut");
    return 0;
}
